# Team Cross Website

独立宣传网站，与 Team Cross 产品仓库分别维护。默认使用简体中文。

- 主张：从具体 Session 出发，保留用户熟悉的终端、原生客户端、工作区和项目组织方式。
- WebGUI 是可选入口。区分个人 Agent 通过 MCP 辅助与原生客户端直接操作。
- 当前下载入口和功能范围以产品仓库及对应 Release 为依据；不要虚构 CLI 命令、兼容认证或验证结论。
- 当前预发布能力包括只读空间、多人加入、多份会话材料和固定版本引用；托管 Agent 自主判断并发起邀请仍标为产品方向。
- 宣传页面讲述用户能做什么及实际使用边界，不展示开发验收环境、测试覆盖或工程门槛。
- Tailcat 与 Claude Code 原生 TUI 的实验性标识保留。
- 区分只读分享与共同执行：只读分享固定公开范围，不创建 fork、不开放执行目录；共同执行创建原生 fork，明确开放访问与交接输入，执行留在发起者主机。
- 不增加账号、追踪、后端存储或外部服务，除非用户要求。
- 未收到发布请求时仅在本地开发，不创建远端项目、推送仓库或部署。
- 沿用项目的 npm 锁文件。检查 `npm run check`、`npm run build` 和真实浏览器交互；不要为静态样式编写镜像实现的测试。
- 页面修改需检查手机、桌面、键盘和减少动态效果，不得出现假按钮或死链接。
- 默认不创建子 Agent。
